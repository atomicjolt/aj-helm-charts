apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atomic-canvas.fullname" . }}-test-connection"
  labels:
    {{- include "atomic-canvas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atomic-canvas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
