apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atomic-reactor.fullname" . }}-test-connection"
  labels:
    {{- include "atomic-reactor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atomic-reactor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
